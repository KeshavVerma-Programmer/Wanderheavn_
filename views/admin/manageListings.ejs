<% layout("/layouts/boilerplate") %>
<div class="container mt-4">
    <h2 class="text-center mb-4 text-uppercase fw-bold">Manage Listings</h2>

    <!-- Search Bar -->
    <div class="row mb-3">
        <div class="col-md-6 offset-md-3">
            <input 
                type="text" 
                id="searchInput" 
                class="form-control" 
                placeholder="Search listings by title or location..."
            />
        </div>
    </div>

    <% if(listings.length > 0) { %>
        <div class="table-responsive">
            <table class="table table-hover table-bordered align-middle" id="listingTable">
                <thead class="table-dark text-center">
                    <tr>
                        <th>Title</th>
                        <th>Location</th>
                        <th>Price (&#8377;)</th>
                        <th>Status</th>
                        <th>Actions</th>
                    </tr>
                </thead>
                <tbody>
                    <% listings.forEach(listing => { %>
                        <tr>
                            <td>
                                <%= listing.title %> 
                                <% if (listing.isFeatured) { %>
                                    <span class="badge bg-info ms-2">Featured</span>
                                <% } %>
                            </td>
                            <td><%= listing.location %></td>
                            <td><%= listing.price.toLocaleString("en-IN") %></td>
                            <td>
                                <span class="badge 
                                    <%= listing.status === 'Active' ? 'bg-success' 
                                    : listing.status === 'Pending' ? 'bg-warning' 
                                    : 'bg-danger' %>">
                                    <%= listing.status %>
                                </span>
                            </td>
                            <td class="text-center">
                                <a href="/admin/listings/<%= listing._id %>/edit" 
                                   class="btn btn-sm btn-primary me-1">
                                    <i class="fas fa-edit"></i> Edit
                                </a>

                                <form action="/admin/listings/<%= listing._id %>?_method=DELETE" 
                                      method="POST" 
                                      class="d-inline"
                                      onsubmit="return confirmDelete('<%= listing.title %>');">
                                    <button class="btn btn-sm btn-danger">
                                        <i class="fas fa-trash"></i> Delete
                                    </button>
                                </form>
                            </td>
                        </tr>
                    <% }) %>
                </tbody>
            </table>
        </div>
    <% } else { %>
        <div class="alert alert-info text-center">
            No listings available.
        </div>
    <% } %>
</div>

<script>
    // Enhanced Delete Confirmation
    function confirmDelete(title) {
        return confirm(`Are you sure you want to delete the listing: "${title}"?`);
    }

    // Search Filter Functionality
    const searchInput = document.getElementById('searchInput');
    searchInput.addEventListener('keyup', function() {
        const filter = searchInput.value.toLowerCase();
        const rows = document.querySelectorAll('#listingTable tbody tr');

        rows.forEach(row => {
            const title = row.cells[0].innerText.toLowerCase();
            const location = row.cells[1].innerText.toLowerCase();

            if (title.includes(filter) || location.includes(filter)) {
                row.style.display = "";
            } else {
                row.style.display = "none";
            }
        });
    });
</script>
